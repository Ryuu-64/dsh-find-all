# dsh-find-all

DSH Desktop 的 **⌘F / Ctrl+F 查找**插件。它有一个和浏览器查找不一样的地方：**搜的是整段会话，不只是屏幕上已经显示出来的那一截。**

## 为什么需要它

DSH 桌面窗口是 Electron 壳，本身没有浏览器那种查找栏。而会话视图一次只在页面里保留最近 50 条消息——点一次「加载更早」才再多 50 条。

所以拿普通查找条搜一个较早的词，结果永远是 0，哪怕这段会话里确实有。

这个插件在你输入关键词之后，通过会话服务把更早的历史一页页加载进来，每加载一页就重搜一次，直到整段会话都在页面里。这样命中计数和高亮覆盖的是**整个会话**。

## 功能

- **⌘F / Ctrl+F** 唤起查找栏，自动带入当前选中的文字
- **范围切换**：`整段`（默认）↔ `本页`，点那个胶囊按钮切换
- **边搜边加载**：整段模式下自动分页加载历史，高亮与计数随加载实时更新
- **进度可见**：显示「正在加载更早的历史… 已 3 页」，**点一下这段文字即可停止加载**
- **安全上限**：最多加载 400 页（约 2 万条消息）；历史不再增长时会自行停止
- **导航**：Enter / Shift+Enter 上下一个；⌘G（Ctrl+G）/ F3 下一个，加 Shift 反向；Esc 关闭查找栏并清除高亮
- 切换会话时自动重置，并在新会话里重新开始搜索

## 安装

用 DSH 自带的插件命令装：

```sh
dsh plugin --profile desktop add @ryuu-64/dsh-find-all
```

装完请**完整重启一次 DSH Desktop**——插件清单只在启动的时候装载，不重启不会生效。

装完按 **⌘F / Ctrl+F**，右上角出现查找栏就算装好了。

### 和 dsh-find-bar 只能留一个

`dsh-find-bar` 和本插件**都会抢 Ctrl+F**，两个同时装会互相顶掉。如果你之前装过它，先卸掉：

```sh
dsh plugin --profile desktop remove dsh-find-bar
```

## 使用

| 操作 | 按键 |
| --- | --- |
| 打开查找栏 | `⌘F` / `Ctrl+F` |
| 下一个 / 上一个命中 | `Enter` / `Shift+Enter` |
| 下一个 / 上一个命中（另一套） | `⌘G`（`Ctrl+G`）/ `F3`，加 `Shift` 反向 |
| 切换范围 `整段` ↔ `本页` | 点查找栏上的胶囊按钮 |
| 停止加载更早的历史 | 点「正在加载更早的历史…」那行文字 |
| 关闭并清除高亮 | `Esc` |

搜索全部在本机完成：插件只向 DSH 的会话服务要更早的历史，不发网络请求，也不改动会话内容。

## 已知限制

- 大小写不敏感，没有「区分大小写」开关
- 单次搜索最多记 5000 个命中
- 折叠起来的工具调用和思考块，正文不在页面里，搜不到——先展开再搜
- 整段模式要真的翻历史，所以很长的会话首次搜索需要等几秒

## 卸载 / 回退

```sh
dsh plugin --profile desktop remove @ryuu-64/dsh-find-all   # 卸载
dsh plugin --profile desktop add dsh-find-bar               # 想换回上游那个插件
```

## 关于这个项目

本插件 fork 自 [secyborg/dsh-find-bar](https://github.com/secyborg/dsh-find-bar)（作者 secyborg，MIT），在上游的基础上加了「整段会话」搜索。上游原有的查找条界面、高亮、`n/N` 计数和键盘导航都保留了下来。

版权与许可为 MIT 双署名：`Copyright (c) 2026 secyborg` 与 `Copyright (c) 2026 Ryuu-64`。上游 README 原样保存在 `README.fork-origin.md`，改动清单与致谢见 `NOTICE.md`。

参与开发、打包发布、以及「Failed to load plugins」报错的排查，见 `CONTRIBUTING.md`。
